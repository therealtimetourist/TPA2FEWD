// create a variable that targets the quote div

// create a variable that targets the figure image

// create a function to get a random quote

    // get random index for the quotes

    // write a string of HTML that includes the quote and the source to the quote div  
    

    // write the image to the figure div. use the source for the ALT text